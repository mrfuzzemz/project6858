<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.privacy.sandbox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <permission 
        android:description="@string/sandbox_loc_desc" 
        android:name="com.privacy.sandbox.SANDBOX_LOCATION" 
        android:protectionLevel="normal" 
        android:label="@string/sandbox_loc_label">
    </permission>

    <uses-permission    
        android:name="com.privacy.sandbox.SANDBOX_LOCATION">
    </uses-permission>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.privacy.sandbox.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver android:name="RequestReceiver" >
            <intent-filter>
                <action android:name="com.privacy.sandbox.REQUEST_VALUE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
